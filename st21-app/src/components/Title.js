import { Typography } from '@mui/material';

const Title = () => (
  <Typography
    variant="h5"
    component="div"
    sx={{
      textAlign: 'center',
      color: 'primary.main',
      fontWeight: 100,
    }}
  >
    Hi.
  </Typography>
);

export default Title;
